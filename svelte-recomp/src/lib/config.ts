// Shared app-wide config/constants.
// GOAL_KG: interim target weight, estimated from height (187cm) using a
// BMI ~26 reference point (reasonable upper-healthy target for a lifter
// carrying real muscle mass, not a lean/cut BMI). This is a starting point,
// not a precision figure — true ideal weight depends on body-fat % and
// muscle mass, which the app doesn't measure yet (photo-based body comp
// estimation is a future feature). Editable here in one place.
export const GOAL_KG = 90;
export const START_KG = 133.5;
